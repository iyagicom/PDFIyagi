# PDFIyagi v1.29.2

![PDFIyagi](pdfiyagi.png)

Windows 및 Linux용 **가볍고 빠른 PDF 편집기**입니다.

PDFIyagi는 PDF 텍스트 수정, 새 텍스트 추가, 도장·서명 이미지 삽입, 개인정보 삭제 마킹, 페이지 관리 및 저장 기능을 제공하며, 저장 후에도 다시 편집할 수 있습니다.

> **개발자(AI 포함)는 코드 수정 전 [`ARCHITECTURE.md`](ARCHITECTURE.md)(자체 vs 외부,
> 절대 재구현 금지 목록)부터 읽을 것.** 모듈 구조는 [`MODULE_MAP.md`](MODULE_MAP.md),
> 함수 호출 흐름은 [`CALLGRAPH.md`](CALLGRAPH.md), 설계 결정 상세는 [`DEVNOTES.md`](DEVNOTES.md).

---

## ✨ 주요 기능

### PDF 편집

* **텍스트 편집** — 기존 PDF 텍스트를 직접 수정
* **재편집 가능** — 저장 후 다시 열어도 텍스트 재편집 가능
* **텍스트 서식** — 굵게, 기울임, 밑줄, 취소선 지원
* **글자 색상** — 원하는 텍스트 색상 선택 가능
* **L-Edit 모드** — 문단 형태의 줄 단위 편집
* **Edit 모드** — 양식 및 문서용 필드 단위 편집

### 이미지 PDF 지원

* **스캔 PDF 텍스트 입력** — 이미지 기반 PDF에 새 텍스트 입력
* **도장·서명 이미지 삽입** — 서명, 직인, 도장 이미지 자유 배치
* **이미지 위 텍스트 입력** — 이미지 위에 텍스트 추가 가능
* **클립보드 이미지 붙여넣기** — 복사한 이미지를 바로 삽입
* **이미지 재편집 가능** — 저장 후 다시 열어도 이동 및 편집 가능

### 마킹 및 보안

* **정보 삭제 마킹** — 민감한 정보를 영구적으로 제거
* **블러 마킹** — 선택 영역 흐림 처리
* **도형 그리기** (v1.28 신규) — 사각형·원·직선·화살표, 선 굵기·색상 조절, 저장 시 벡터로 기록

### 저장 (v1.29 신규 — IMGPDF 자체 엔진화)

* **IMGPDF 자체 엔진 저장** — QPdfWriter/PDFium 없이 배경(JPEG)·클립 이미지(독립 오브젝트)·
  텍스트 편집(벡터 외곽선)을 직접 조립. 사진 한 장을 추가해도 그 페이지만, 그 사진 자체의
  크기만큼만 커짐 (이전엔 문서 전체가 재래스터화되며 폭증하던 문제 해결)
* **회전 저장 / OCR 없는 검색 텍스트 레이어**만 있는 경우는 기존 PDFium 경로로 자동 폴백

### 신경망 OCR (v1.24 신규)

* **PP-OCRv5 신경망 엔진 내장** — ONNX Runtime CPU 실행, 페이지당 1초 미만
* **높은 인식률** — 실제 계약서 스캔 기준 오인식 사실상 0 (tesseract 대비 압도적)
* **다국어** — 한국어+영어 / 중국어+일본어+영어 / 서유럽어(라틴), 스캔 시 선택
* **모델 번들** — tesseract 등 외부 패키지 설치 불필요, 어떤 PC에서도 동일 품질
* **검색 가능한 PDF/AI7** — OCR 텍스트 레이어가 저장돼 뷰어에서 검색·선택 가능

## 🧠 AI7 — Seven Capabilities of an AI-Native Document

AI7은 단순한 문서 포맷이 아닙니다. AI가 문서를 읽고, 이해하고, 활용하기 위해 필요한
**7가지 핵심 능력**을 문서 자체에 담은 AI-Native Document Format입니다.

| # | 능력 | 설명 | 구현 |
|---|------|------|------|
| 1 | **Read** | 텍스트·표·이미지·메타데이터를 하나의 문서 객체로 정확하게 읽는다 | Layer 0 래스터 + Layer 1 텍스트 블록 (신경망 OCR) |
| 2 | **Understand** | 문단·제목·표·그림·관계를 의미 단위로 해석한다 | `content/document.md` 시맨틱 마크다운 |
| 3 | **Structure** | 문서를 의미론적 구조로 표현한다 | 표 자동 감지 → `content/table_*.csv`, 이미지 메타데이터 |
| 4 | **Connect** | 지식그래프로 객체와 관계를 연결해 지식 네트워크를 만든다 | `ai/document.kg` (패턴 + LLM 옵션) |
| 5 | **Search** | 키워드가 아니라 의미와 구조로 찾는다 | `embeddings/` 내장 벡터 (로컬 임베딩, RAG-ready) |
| 6 | **Reason** | 관계를 바탕으로 요약·비교·분석·질의응답을 수행한다 | document.md + kg + 임베딩의 조합 (Claude API 연동 옵션) |
| 7 | **Act** | AI 에이전트가 문서를 편집·분석·변환·자동화한다 | 비파괴 오버레이 + `history/` 이력 (agent_scripts는 보안 검토 중) |

**Read → Understand → Structure → Connect → Search → Reason → Act**

> AI7은 문서를 저장하는 파일이 아니라,
> **AI가 이해하고 활용할 수 있는 지식(Knowledge)으로 변환하는 문서 아키텍처입니다.**

---

### AI7 (.ai7) — AI-Native 문서 포맷

* **비파괴 편집** — 페이지 래스터(Layer 0) + 텍스트(Layer 1) + 편집 오버레이(Layer 2)
* **시맨틱 마크다운** — `content/document.md`: 제목/문단이 구조화된 문서 전체 Markdown,
  AI/RAG가 파싱 없이 바로 소비
* **표 구조 추출** — 표를 자동 감지해 `content/table_*.csv` + 마크다운 표로 저장 (v1.25 신규)
* **표 셀 단위 편집** — Edit 모드는 셀 단위, L-Edit 모드는 행 단위로 클릭·수정 (v1.25 신규)
* **글자 크기 자동 추정** — 박스 높이 + 글자 폭 교차 추정으로 원문 크기 그대로 편집
* **빠른 저장** — 페이지 인코딩 병렬화 + 스캔 OCR 결과 재사용 (다중 페이지도 수 초)
* **포맷 명세서** — [AI7FORMAT.md](AI7FORMAT.md) 참조 (컨테이너 구조·레이어·호환성 규칙)

### 스캐너 (Windows + Linux)

* **ADF 다중 페이지 스캔** — 자동급지 장치(ADF)를 이용한 연속 스캔, 장마다 즉시 페이지 생성
* **스캔 품질** — 300/600/1200/2400/4800 DPI 선택
* **OCR (텍스트 인식)** — 내장 신경망 OCR로 검색 가능한 AI7 저장
* **벡터 변환** — 스캔 이미지를 선화 벡터로 변환 (흑백/회색조 모드, Linux)
* **테스트 완료 기종**: Samsung (SL-T1672DW 등), HP (Samsung OEM 기반)
* **지원 프로토콜**: Windows는 WIA, Linux는 eSCL (AirScan)/SANE — 같은 장치가 둘로
  잡히면 AirScan 권장(자동 우선)

> Linux의 ADF 다중 페이지 스캔은 `scanimage` (`sane-utils` 패키지) 설치 시 자동으로 활성화됩니다.

### 생산성

* **실행취소 / 다시실행** — Ctrl+Z, Ctrl+Y 지원
* **Ctrl+드래그 복제** — 텍스트 및 이미지 객체 복제
* **Ctrl+X 잘라내기** — 선택 이미지를 클립보드로 잘라내기
* **이미지 회전** — 선택 이미지 90° 회전 (Ctrl+← / Ctrl+→)
* **텍스트 추출** — 현재 페이지 또는 전체 문서 텍스트 추출
* **문서 전체 글꼴 변경** — 문서 내 모든 글꼴 일괄 변경
* **페이지 관리** — 페이지 순서 변경 및 빈 페이지 삽입
* **썸네일 탐색** — 썸네일을 이용한 빠른 페이지 이동
* **창 크기 기억** — 이전 창 크기·위치 자동 복원

### 렌더링 엔진

* **IYAGI** — 자체 개발 PDF 엔진 (기본값). 외부 PDF 라이브러리 없이 파싱·렌더링·텍스트
  추출·저장(증분 업데이트 방식)까지 전부 자체 구현
* **PDFium / Qt** — IYAGI가 처리하지 못하는 문서를 위한 폴백 엔진 (자동 전환)

---

## 🎮 단축키

| 키                | 기능              |
| ---------------- | --------------- |
| Ctrl + O         | PDF 열기          |
| Ctrl + S         | PDF 저장          |
| Ctrl + Shift + S | 다른 이름으로 저장      |
| Ctrl + B         | 빈 페이지 삽입        |
| Ctrl + I         | 이미지 삽입          |
| Ctrl + V         | 클립보드 이미지 붙여넣기   |
| Ctrl + Z         | 실행취소            |
| Ctrl + Y         | 다시실행            |
| Delete           | 선택 객체 삭제        |
| Ctrl + T         | 현재 페이지 텍스트 추출   |
| Ctrl + Shift + T | 전체 문서 텍스트 추출    |
| Ctrl + Shift + F | 전체 문서 글꼴 변경     |
| Ctrl + 마우스 휠     | 확대 / 축소                    |
| 마우스 휠            | 페이지 스크롤 (경계에서 3클릭 → 페이지 전환) |
| ← / →            | 이전 페이지 / 다음 페이지           |
| Ctrl + ← / →     | 선택 이미지 90° 회전             |
| 방향키              | 선택 객체 1픽셀 이동              |
| Shift + 방향키      | 선택 객체 10픽셀 이동             |
| Ctrl + 드래그       | 텍스트 또는 이미지 복제             |
| Ctrl + X         | 선택 이미지 잘라내기               |

---

## ⬇ 다운로드

### Windows

Microsoft Store에서 설치할 수 있습니다.

### Linux

GitHub Releases에서 최신 버전을 다운로드할 수 있습니다.

```bash
chmod +x PDFIyagi
./PDFIyagi
```

---

## 🖥 지원 플랫폼

* Windows 10 / 11
* Linux (Ubuntu, Debian 및 호환 배포판)

---

## 👤 제작

IYAGI INC

Email: [iyagicom@gmail.com](mailto:iyagicom@gmail.com)

GitHub: https://github.com/iyagicom

---

## 📋 버전 이력

### v1.29.0 (2026-07-09) — 새로운 IMGPDF 저장 엔진
✨ IMGPDF 저장 엔진 전면 개선
PDFIyagi만의 새로운 IMGPDF 저장 엔진을 적용했습니다.
사진, 도장 등 이미지를 추가해도 파일 크기가 불필요하게 크게 증가하는 문제를 해결했습니다.
삽입한 이미지는 원본 해상도를 유지하면서 저장되어 화질 저하를 최소화했습니다.
배경과 삽입 이미지를 독립적으로 저장하여 다른 페이지나 배경 이미지가 다시 저장되는 현상을 개선했습니다.
텍스트 편집 내용은 벡터 방식으로 저장되어 확대해도 선명한 품질을 유지합니다.
저장 엔진 구조를 개선하여 안정성과 유지보수성을 높였습니다.
새로운 엔진에서 지원하지 않는 일부 기능(검색 가능한 OCR 텍스트 등)은 자동으로 기존 방식으로 처리하여 호환성을 유지합니다.
다양한 PDF 뷰어에서 동일한 결과를 제공하도록 저장 및 렌더링 검증 기능을 추가했습니다.

### v1.28.0 — 도형 그리기
PDF에 다양한 도형을 직접 그릴 수 있습니다.
사각형, 원, 직선, 화살표 그리기를 지원합니다.
선 굵기를 자유롭게 조절할 수 있으며, 그리는 동안 실시간으로 미리보기를 제공합니다.
도형은 벡터 방식으로 저장되어 확대해도 깨지지 않는 선명한 품질을 유지합니다.
AI7 문서 형식에서도 도형을 저장하고 다시 편집할 수 있습니다.

### v1.25.x (2026-07-03 ~ 07-04)
- **AI7 시맨틱 레이어 완성 — Seven Capabilities 전 단계 구현**
  - 표 자동 감지 → `content/table_*.csv` + 마크다운 표, Edit 모드 셀 단위 / L-Edit 모드
    행 단위 편집
  - 이미지 메타데이터(크기·DPI·인코딩·sha256), 글자 크기 자동 추정
  - `embeddings/` 내장 벡터 — 로컬 임베딩 모델 번들, 의미 기반 검색(RAG-ready)
  - `history/` 편집 이력 — 저장할 때마다 직전 상태 보존 (최근 20개)
  - `ai/document.kg` 지식그래프 — 인명/금액/날짜 등 자동 추출 (LLM 연동 옵션)
- 다중 페이지 저장 병렬화 — 페이지 인코딩 동시 실행으로 수 초 내 저장
- 인쇄 화질 개선 및 페이지 누락 방지
- 문서 정보 패널, 개인정보(주민번호 등) 자동 마킹, 페이지 회전 상태 저장
- 텍스트를 클릭만 해도 편집된 것으로 기록되던 버그 수정
- **Windows 완전 지원** (v1.25.12~13) — 신경망 OCR·시맨틱 검색의 Windows 빌드 지원,
  스캔 개선(장마다 즉시 페이지 생성, OCR 선택 시 AI7 자동 지정, 첫 스캔 안정화)

### v1.24.x (2026-07-03)
- **신경망 OCR(PP-OCRv5) 내장** — ONNX Runtime CPU 실행, 모델 번들로 외부 설치 불필요,
  실제 계약서 스캔 기준 오인식 사실상 0. 한국어+영어 / 중국어+일본어+영어 / 라틴 선택
- **AI7 포맷 명세서(AI7FORMAT.md) 공개** — 컨테이너 구조·레이어·좌표계·호환성 규칙
- `content/document.md` 시맨틱 마크다운 — 제목/문단 구조화된 문서 전체 Markdown 자동 생성
- 스캔 배경 무손실(WEBP lossless) 저장, 다중 스캔 유실 버그 등 안정화 다수

### v1.23.x (2026-07-02 ~ 07-03)
- PDF 텍스트 편집 저장을 PDFium 임포트 방식에서 자체 "증분 업데이트(incremental
  update)" 방식으로 전환 — 원본 바이트를 건드리지 않고 새 오브젝트만 덧붙이는 PDF
  스펙 표준 기법. 편집한 글자는 폰트 파일을 통째로 임베딩하지 않고 실제 사용된 글자의
  벡터 외곽선만 그려서, 저장할 때마다 용량이 수십 MB로 불어나던 문제도 함께 해결
  (75KB 문서 편집 저장 시 15MB → 87.7KB)
- IYAGI 엔진의 PDF 텍스트 추출(검색/복사/클릭편집)이 글리프 하나하나를 따로 반환하던
  것을 실제 줄/단어 단위로 재구성하도록 수정 — 일부 문서(예: Google Docs로 만든 PDF)는
  글자마다 별도로 위치를 지정해서 저장하는데, 이런 문서에서 텍스트가 글자 단위로
  쪼개져 나오던 문제
- AI7(자체 편집 포맷) 변환 시 원본에 이미 텍스트 레이어가 있으면 OCR을 돌리지 않고
  그 텍스트를 그대로 재사용하도록 변경 — 실제 텍스트가 있는데도 매번 OCR을 다시
  돌려서 인식 오류가 섞여 들어가던 문제 해결. 텍스트 레이어가 없는 진짜 스캔 문서는
  기존대로 OCR 사용
- AI7 텍스트 편집 시 삭제한 내용이 페이지 이동 후 되살아나던 버그, OCR 텍스트 줄 묶기가
  제목/본문처럼 글자 크기가 섞인 문서에서 깨지던 버그 수정
- 엔진 선택 목록에 실제 엔진 이름(IYAGI/PDFium/Qt/WinRT) 표시

### v1.21.x ~ v1.22.x (2026-07-01 ~ 07-02)
- **자체 PDF 렌더링 엔진(IYAGI) 개발 및 기본 엔진 승격** — 그동안 의존하던 PDFium이
  특정 문서(CAD 도면류 등)에서 비결정적으로 크래시하는 문제를 근본적으로 해결하기 위해
  PDF 파서·문법 검증기·벡터/텍스트/이미지/어노테이션 렌더러·텍스트 추출을 전부 자체
  구현. 실제 고객 PDF 2,000여 개로 검증(크래시 0건) 후 기본 엔진으로 전환, PDFium/Qt는
  폴백으로만 유지
- **깨진 PDF 관용 처리** — 잘린 파일, 잘못된 오프셋/길이, 순환 참조가 있는 손상 문서도
  크래시나 무한 대기 없이 읽을 수 있는 부분만 표시
- **한글 CID 폰트 호환성 대량 수정** — 임베디드 한글 폰트 로드 실패, 한글 코드가
  유니코드로 저장된 문서(UniKS-UCS2 등), 폰트 이름이 깨진 문서의 대체 폰트 선택
  (한/중/일 문자 집합 기반) 등 실제 관공서·업무 문서에서 글자가 안 보이던 문제들
- **암호화 PDF 자동 감지** — 암호 걸린 문서는 암호 입력을 지원하는 엔진으로 자동 전환
- 배포판에 JPEG/WebP 이미지 디코더가 빠져 위성사진 등 이미지가 안 보이고 AI7 저장이
  백지가 되던 문제 수정
- .ai7 파일을 "파일 열기"에서 직접 열 수 있게 수정, AI7 재편집 시 첫 페이지 편집 내용이
  사라지던 버그·OCR 텍스트가 클릭되지 않던 버그·편집 영역이 단어 단위로 조각나던 버그,
  텍스트 추출이 .ai7에서 동작하지 않던 버그 수정

### v1.20.28 (2026-06-19)
- Windows 버전 출시
- 파일 열기 창이 파일이야기(FileIyagi)와 연동되지 않던 문제 수정
- 빈 페이지를 추가했을 때 이전 페이지 내용이 유령처럼 보이던 버그 수정

### v1.20.16
- 편집 내용을 별도 파일(.ai7)로 저장 가능 — 다시 열면 텍스트·이미지 편집 이어서 가능
- ADF 자동급지 스캔 시 여러 장이 한 장만 들어오던 버그 수정
- 스캔 중 페이지 이동, 스크롤 관련 버그 수정

### v1.18.18
- 툴바 아이콘 새로 교체

### v1.18.5
- 페이지를 이동했을 때 이전 페이지 내용이 새 페이지에 겹쳐 보이던 버그 수정
- 텍스트·이미지를 끌어서 옮길 때 이동될 위치 미리보기 표시
- OCR 인식률 개선

### v1.17.9
- ADF 자동급지 여러 장 연속 스캔 안정화

---

## 📜 라이선스

Copyright (c) 2026 IYAGI INC. All rights reserved.

본 소프트웨어는 실행 파일 형태로만 배포됩니다. 소스 코드는 공개되지 않습니다.

### Linux 버전

개인, 상업, 교육, 정부, 기관 등 모든 용도로 자유롭게 사용, 설치, 패키징 및 재배포할 수 있습니다.

### Windows 버전

Microsoft Store를 통해 배포됩니다.

사용 및 라이선스 정책은 Microsoft Store 정책을 따릅니다.
